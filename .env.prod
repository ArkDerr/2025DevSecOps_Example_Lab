# Imágenes (puedes cambiarlas por si varían entre entornos)
DOCKER_IMAGE_BACKEND=profedanielduoc/mi-backend
DOCKER_IMAGE_FRONTEND=profedanielduoc/mi-frontend

# Qué tag desplegar (usa latest, un tag semántico o el sha calculado por Actions)
APP_TAG=latest
# APP_TAG=sha-abcdef1
# APP_TAG=v1.0.0

# Backend
API_KEY=9P9ZtXrN8NZXi0IMsT5d
SPRING_PROFILE_ACTIVE=prod

# Frontend / Django
DJANGO_ALLOWED_HOSTS=tu-dominio.cl,localhost,127.0.0.1
WEB_CONCURRENCY=2
GUNICORN_TIMEOUT=60

# (Opcional) URL del backend expuesta al front
# BACKEND_BASE_URL=http://backend:8080
